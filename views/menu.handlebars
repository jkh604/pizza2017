<div class="row">
{{#each infoMenu}}
<div class="col-md-4">
		<div class="text-center">
		<p>{{name}}</p>
		<img src="./images/{{img}}" alt="pizza" height="150" width="150">
		</div>
		<div class="text-center">
		<button type="button" class="btn btn-primary" onclick="addToCart({{@index}})">Add</button>
		</div>
	</div>
</div>
{{/each}}